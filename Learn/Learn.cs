// 这里是注释 下面的是引入命名空间
using System;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using System.Collections.Generic;
//定义命名空间 从{开始，到}结束
namespace Learn
{
    //定义类
    static void Ahsdf()//括号内是参数
    {
        //方法体
        int age = 20;
        int hp = 60;
        string name = "jzfwan";
        Console.WriteLine(age);
    }
}
